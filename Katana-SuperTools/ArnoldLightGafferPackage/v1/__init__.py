import os
import PackageSuperToolAPI

# Import Package modules
import ArnoldLightGafferPackage

# Import UIDelegate modules if we are running in UI mode
if PackageSuperToolAPI.IsUIMode():
    import ArnoldLightGafferUIDelegate